apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "led-sniffer.fullname" . }}
  labels:
    {{- include "led-sniffer.labels" . | nindent 4 }}
spec:
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: {{ .Values.updateStrategy.maxUnavailable | default 1 }}
  minReadySeconds: {{ .Values.minReadySeconds | default 10 }}
  selector:
    matchLabels:
      {{- include "led-sniffer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "led-sniffer.labels" . | nindent 8 }}
    spec:
      affinity:
        nodeAffinity:
          {{- toYaml .Values.global.nodeAffinity | nindent 10 }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.hostNetwork }}
      # hostNetwork is required so scapy can sniff the cni0 bridge in the
      # host network namespace (flannel's pod bridge). Without it, the pod
      # only sees its own veth side and misses pod-to-pod traffic.
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds | default 30 }}
      containers:
        - name: led-sniffer
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" }}
          securityContext:
            # CAP_NET_RAW is required for scapy's pcap-based capture, and
            # the NeoPixel driver pokes /dev/mem directly — both require
            # full privileged mode.
            privileged: {{ .Values.privileged }}
          args:
            {{- if .Values.verbose }}
            - "--verbose"
            {{- end }}
            {{- if .Values.source }}
            - "--source"
            - "{{ .Values.source }}"
            {{- end }}
            {{- if .Values.port }}
            - "--port"
            - "{{ .Values.port }}"
            {{- end }}
            {{- if .Values.interface }}
            - "--interface"
            - "{{ .Values.interface }}"
            {{- end }}
            {{- if .Values.pod_cidr }}
            - "--pod-cidr"
            - "{{ .Values.pod_cidr }}"
            {{- end }}
            - "--threshold"
            - "{{ .Values.threshold }}"
            - "--delay"
            - "{{ .Values.delay }}"
            - "--filter_size"
            - "{{ .Values.filter_size }}"
            - "--led-count"
            - "{{ .Values.led_count }}"
            - "--led-pin"
            - "{{ .Values.led_pin }}"
            - "--animation-color"
            - "{{ .Values.animation_color }}"
            - "--animation-speed"
            - "{{ .Values.animation_speed }}"
            - "--animation-spacing"
            - "{{ .Values.animation_spacing }}"
            {{- with .Values.extraArgs }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if and .Values.startupProbe .Values.startupProbe.enabled }}
          # Gates the liveness probe until the first health file touch.
          startupProbe:
            exec:
              command: ["test", "-f", "/tmp/healthy"]
            periodSeconds: {{ .Values.startupProbe.periodSeconds | default 10 }}
            failureThreshold: {{ .Values.startupProbe.failureThreshold | default 30 }}
            timeoutSeconds: {{ .Values.startupProbe.timeoutSeconds | default 3 }}
          {{- end }}
          {{- if and .Values.livenessProbe .Values.livenessProbe.enabled }}
          # Touched every supervisor iteration (success or capture error).
          # A stale marker means the loop itself is hung.
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - 'test -f /tmp/healthy && test $(( $(date +%s) - $(stat -c %Y /tmp/healthy) )) -lt {{ .Values.livenessProbe.stalenessSeconds | default 30 }}'
            periodSeconds: {{ .Values.livenessProbe.periodSeconds | default 15 }}
            failureThreshold: {{ .Values.livenessProbe.failureThreshold | default 2 }}
            timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds | default 3 }}
          {{- end }}
          {{- with .Values.devices }}
          volumeMounts:
            {{- range . }}
            - name: {{ .name }}
              mountPath: {{ .hostPath }}
            {{- end }}
          {{- end }}
      {{- with .Values.devices }}
      volumes:
        {{- range . }}
        - name: {{ .name }}
          hostPath:
            path: {{ .hostPath }}
            {{- if .type }}
            type: {{ .type }}
            {{- end }}
        {{- end }}
      {{- end }}
